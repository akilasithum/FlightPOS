<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.pos.flightpos">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".FlightAttendentLogin"
            android:label="@string/title_activity_flight_attendent_login" />
        <activity android:name=".AttendendMainActivity" />
        <activity android:name=".SyncActivity" />

        <service
            android:name="com.amazonaws.mobileconnectors.s3.transferutility.TransferService"
            android:enabled="true" />

        <activity android:name=".PrintActivity" />
        <activity android:name=".AttCheckInfo" />
        <activity android:name=".ICCardReader" />
        <activity android:name=".MSReader" />
        <activity android:name=".AddSeal" />
        <activity android:name=".SellarInfoActivity" />
        <activity android:name=".SellItemsActivity" />
        <activity android:name=".BuyItemsFromServiceTypeActivity" />
        <activity android:name=".PaymentMethodsActivity" />
        <activity android:name=".InventoryReportActivity" />
        <activity android:name=".VerifyInventoryActivity" />
        <activity android:name=".CheckInventoryActivity" />
        <activity android:name=".VerifyDrawerActivity"></activity>
    </application>

</manifest>